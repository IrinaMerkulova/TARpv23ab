SQL Serveris tekkivad vead

1. 
create room_ype(
id int primary key identity(1,1),
description varchar(80),
max_capacity int)

Unknown object type 'room_ype' used in a CREATE, DROP, or ALTER statement.
on vaja kirjutada  - create TABLE room_ype( 
-------
2.
create table room_type(
id int primary key identity(1,1),
description (80),
max_capacity int)
select * from room_type;

The definition for column 'description' must include a data type.
on vaja kirjutada -  description varchar(80),
---------
3. 
insert into room_type(description, max_capacity)
values('Super Luxury Suite', 2),

Incorrect syntax near ','.
kõppus kirjutatakse ; - values('Super Luxury Suite', 2);
4.
foreign key (room_id) room(id);
on vaja kirjutada references -  foreign key (room_id) references room(id)
-------
5.
create table occupied_room(
id int pramiry key identity(1,1),
check_in date,

Incorrect syntax near 'pramiry'.
pramiry key - primary key

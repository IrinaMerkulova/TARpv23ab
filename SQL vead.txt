SQL Serveris tekkivad vead

1. 
create room_ype(
id int primary key identity(1,1),
description varchar(80),
max_capacity int)

Unknown object type 'room_ype' used in a CREATE, DROP, or ALTER statement.
on vaja kirjutada  - create TABLE room_ype( 
-------
2.
create table room_type(
id int primary key identity(1,1),
description (80),
max_capacity int)
select * from room_type;

The definition for column 'description' must include a data type.
on vaja kirjutada -  description varchar(80),
---------
3. 
insert into room_type(description, max_capacity)
values('Super Luxury Suite', 2),

Incorrect syntax near ','.
kõppus kirjutatakse ; - values('Super Luxury Suite', 2);
4.
foreign key (room_id) room(id);
on vaja kirjutada references -  foreign key (room_id) references room(id)
-------
5.
create table occupied_room(
id int pramiry key identity(1,1),
check_in date,

Incorrect syntax near 'pramiry'.
pramiry key - primary key
------
6.
create procedure AddGuest -- see funktsioon on külalise lisamiseks
    @first_name varchar(80),
    @last_name varchar(80),
    @member_since date
begin
    insert into guest (first_name, last_name, member_since)
    values(@first_name, @last_name, @member_since);
end; 

Incorrect syntax near the keyword 'begin'.
enne 'begin' kirjutatakse 'as'
------
7.
AddGuest 'Alice', 'Johnson', '2022-09-28';

Could not find stored procedure 'AddGuest'.

on vaja kirjutada nii - EXEC AddGuest 'Alice', 'Johnson', '2022-09-28';
-------
8.
create procedure AddReservation -- see funktsioon on ette nähtud broneeringu lisamiseks 
date_in date,
date_out date,
made_by varchar(20),
guest_id int
as
begin
	insert into reservation (date_in, date_out, made_by, guest_id)
	values(@date_in, @date_out, @made_by, @guest_id)
end;

Incorrect syntax near 'date_in'

enne kirjutatakse @ -
@date_in date,
@date_out date,
@made_by varchar(20),
@guest_id int 
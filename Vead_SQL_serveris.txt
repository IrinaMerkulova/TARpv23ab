Vead SQL Serveris.
1.
CREATE TABL kool(
id int primary key,
koolNimi varchar(50),
koolAasta int);

Unknown object type 'TABL' used in a CREATE, DROP, or ALTER statement.
Жалуется на неизвестный объект после оператора CREATE

2.
CREATE TABLE kool(
id int primary key,
koolNimi varchar(50),
koolAasta);

The definition for column 'koolAasta' must include a data type.
Последний столбец не содержит тип данных

3.
INSERT INTO kool(id, koolNimi, koolAasta)
VALUES (1, 'TTHK', 'kolm');

Conversion failed when converting the varchar value 'kolm' to data type int.
Не смог записать текст в столбце где должно быть int-овое значение

4.
INSERT INTO kool(id, koolNimi, koolAasta)
VALUES (1, TTHK, 2010);

Invalid column name 'TTHK'.
Не может добавить запись из-за отсутсвия ковычек

5.
INSERT INTO kool(id, koolNimi, koolAasta)
VALUES (2, 'TTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHK', 2010);

String or binary data would be truncated in table 'SQL Serveris tekkivad vead.dbo.kool', column 'koolNimi'. Truncated value: 'TTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTTHKTT'
Большой текст не соответствует ограничению в varchar

6.
ORDER BY koolAasta DESC;

Incorrect syntax near the keyword 'ORDER'.
Неправильный синтаксис после ключевого слова

7.
SELECT * FROM notkool

Invalid object name 'notkool'
Не может выбрать таблицу так как она не существует

8.
GRANT SELECT, INSERT ON kool to roman;

Cannot find the user 'roman', because it does not exist or you do not have permission.
Не может выдать права пользователю так как он не существует

9.
CREATE TABLE test(
id int primary key)

CREATE TABLE permission denied in database 'SQL Serveris tekkivad vead'
Не дает создать таблицу так как у пользователя нет прав

10.
DELETE FROM kool
WHERE id = 1

The DELETE permission was denied on the object 'kool', database 'SQL Serveris tekkivad vead', schema 'dbo'
Для пользователя запрещено удалять таблицу

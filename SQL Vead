SQL Serveris tekkivad vead

1. 
create room_type(
id int primary key identity(1,1),
description varchar(80),
max_capacity int);

Unknown object type 'room_type' used in a CREATE, DROP, or ALTER statement.
-- probleem Tundmatu objekti tüübiga selles veas on viga, et käsk ei ole lõpuni täidetud. - Create table room_type(...

2. 
CREATE table room_type(
id int PRIMARY KEY identity(1,1),
description varchar(80),
max_capacity int);

There is already an object named 'room_type' in the database.
-- See viga näitab, et selle nimega tabel on juba olemas, kui olete tabeli loonud õigete omadustega, võite julgelt tööd jätkata, kuid kui omadused ei vasta soovitud omadustele, saate tabeli kustutada, kasutades (Drop table ...).

3.
INSERT INTO room_type (description , max_capacity) 
VALUES (1,'standard');
Conversion failed when converting the varchar value 'standard' to data type int.
-- See viga näitab, et andmed ei ole kirjutatud õiges järjekorras ja sql päring loeb 'standard' kui int väärtust.

4. 
CREATE TABLE room(
id int Primary key identity(1,1),
number varchar(10),
name varchar(40)
status varchar(10),
smoke bit,
room_type_id int,
foreign key (room_type_id) references room_type(id));

Incorrect syntax near 'smoke'.
-- See viga näitab, et päringus puudub koma pärast sõna 'smoke', sest probleem on süntaksis.

5.
SELECT * FROM reservation;

Invalid object name 'reservation'
-- See viga näitab, et reservation tabel on puudub 

6. 
CREATE TABLE guest(
id int Primary key identity(1,1),
first_name varchar(256),
last_name nvarchar(80),
member_since date);

INSERT INTO guest(id, first_name, last_name, member_since)
VALUES(4,'Timur', 'Basirov', '2024-05-11');
Cannot insert explicit value for identity column in table 'guest' when IDENTITY_INSERT is set to OFF.
-- See viga näitab, et te ei saa sisestada andmeid tabelisse, milles on 'identiteet'.

7.
CREATE TABLE guest(
id int Primary key identity (1,1),
first_name veaform (80),
last_name veaform (80),
member_since date);

Column, parameter, or variable #2: Cannot find data type veaform.
-- See viga näitab, et andmeformaat ei vasta olemasolevale andmeformaadile. 

8.
create table reservation(
id int primary key identity(1,1),
date_in date,
date_out date,
made_by varchar (20),
guest_id int);

CREATE TABLE permission denied in database 'master'.
-- See viga ütleb, et te töötate teises andmebaasis
